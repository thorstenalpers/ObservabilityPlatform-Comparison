<Window x:Class="TelemetryApp.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Telemetry WPF Client" Height="450" Width="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Create Error"
                    Width="150"
                    Margin="0,0,10,0"
                    Click="CreateError_Click"/>

            <Button Content="Make Request GetWeatherforecast"
                    Margin="0,0,10,0"
                    Width="150"
                    Click="MakeRequestGetWeatherforecast_Click"/>

            <Button Content="Make Request GetException"
                    Margin="0,0,10,0"
                    Width="150"
                    Click="MakeRequestGetException_Click"/>
        </StackPanel>

        <TextBox x:Name="OutputBox"
                 Grid.Row="1"
                 Height="200"
                 Margin="0,10,0,0"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"/>
    </Grid>
</Window>