apiVersion: v1
kind: Service
metadata:
  name: openobserve-nodeport
  namespace: default
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: openobserve 
  ports:
    - name: http
      protocol: TCP
      port: 5080
      targetPort: 5080
      nodePort: 30117